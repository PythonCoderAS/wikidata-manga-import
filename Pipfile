[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
requests-cache = "*"
beautifulsoup4 = "*"
wikidata-bot-framework = ">=7.4.0"

[dev-packages]
ruff = "*"
pre-commit = "*"

[scripts]
lint = "ruff check . --fix"
lint-ci = "ruff check ."
format = "ruff format ."
format-ci = "ruff format . --check"

[requires]
python_version = "3.12"
